import { Request, Response } from "express"

export const joinRoomWithID = (req: Request, res: Response) => {}

export const joinRoomWithoutID = (req: Request, res: Response) => {
    const { user } = req
    
}
